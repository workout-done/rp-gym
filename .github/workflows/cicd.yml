name: CI/CD

on:
  pull_request:
    branches: ["develop", "main"]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".github/ISSUE_TEMPLATE/**"

  push:
    branches: ["main"]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".github/ISSUE_TEMPLATE/**"

# 같은 브랜치에서 새로운 커밋이 올라오면
# 이전 CI를 취소하여 불필요한 리소스 사용을 방지
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# CI에서 Repository의 내용을 읽기만 할 수 있도록 권한 제한
permissions:
  contents: read

jobs:
  # Build & Test
  build_and_test:
    name: Build & Test

    # GitHub Actions에서 제공하는 Ubuntu 환경에서 실행
    runs-on: ubuntu-latest

    # CI가 15분 이상 실행되면 강제 종료
    timeout-minutes: 15

    steps:
      # 소스 코드 가져오기
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          # 변경된 파일을 비교하기 위해 전체 Git 히스토리를 가져옴
          fetch-depth: 0

      # Java 환경 설정
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          # Eclipse Temurin JDK 사용
          distribution: temurin

          # 프로젝트에서 사용하는 Java 버전과 동일하게 17 사용
          java-version: "17"

          # Gradle 의존성 캐시 사용하여 빌드 시간 단축
          cache: gradle

      # Gradle 설정
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

        # Gradle 의존성 및 빌드 캐시를 관리하여
        # 반복되는 CI 빌드 시간을 줄임

      # PR인 경우 변경된 파일 확인
      # tj-actions/changed-files: 변경된 파일과 디렉터리를 찾아주는 Action
      # 버전 태그 대신 특정 커밋으로 고정하여 동일한 코드가 실행되도록 설정
      - name: Get changed files
        if: ${{ github.event_name == 'pull_request' }}
        id: changed-files
        uses: tj-actions/changed-files@9426d40962ed5378910ee2e21d5f8c6fcbf2dd96 # v47.0.6

      # 변경된 모듈 분석
      - name: Determine affected modules
        id: analyze
        env:
          EVENT_NAME: ${{ github.event_name }}

          # PR에서 변경된 모든 파일 경로
          CHANGED_FILES: ${{ steps.changed-files.outputs.all_changed_files }}

        shell: bash

        run: |
          # main에 push된 경우
          # main은 최종 통합 코드이므로
          # 특정 모듈만이 아니라 전체 프로젝트를 검증
          if [[ "$EVENT_NAME" == "push" ]]; then
            echo "TASKS=build" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # 전체 빌드가 필요한지 여부
          RUN_ALL=false

          # 변경된 모듈을 저장할 배열
          MODULES=()

          # 변경된 파일을 하나씩 확인
          for file in $CHANGED_FILES; do

            # 공통 모듈 또는 루트/CI 설정이 변경된 경우
            # 전체 모듈에 영향을 줄 수 있으므로 전체 build
            case "$file" in
              common/*|\
              settings.gradle|\
              build.gradle|\
              gradle/*|\
              gradlew|\
              gradlew.bat|\
              .github/workflows/*)

                RUN_ALL=true
                break
                ;;
            esac

            # 변경된 파일의 첫 번째 디렉터리를 모듈명으로 사용
            #
            # 예:
            # user-service/src/...      -> user-service
            # discovery-server/src/...  -> discovery-server
            module=$(echo "$file" | cut -d/ -f1)

            # 실제 Gradle 모듈인지 확인
            if [[ -f "$module/build.gradle" ]]; then
              MODULES+=("$module")
            fi

          done

          # 전체 빌드가 필요한 경우
          if [[ "$RUN_ALL" == true ]]; then
            echo "TASKS=build" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # 중복된 모듈 제거
          UNIQUE=$(printf "%s\n" "${MODULES[@]}" | sort -u)

          # 애플리케이션 모듈 변경이 없는 경우
          # 예:
          # README만 수정
          # docs만 수정
          #
          # 이 경우 빌드할 Gradle 모듈이 없으므로 종료
          if [[ -z "$UNIQUE" ]]; then
            echo "TASKS=none" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          TASKS=""

          # 변경된 모듈만 Gradle build task 생성
          #
          # 예:
          # user-service
          # ->
          # :user-service:build
          while IFS= read -r module
          do
            [[ -z "$module" ]] && continue

            TASKS="$TASKS :$module:build"

          done <<< "$UNIQUE"

          echo "TASKS=$TASKS" >> "$GITHUB_OUTPUT"

      # Gradle 실행 권한 설정
      - name: Grant execute permission
        if: ${{ steps.analyze.outputs.TASKS != 'none' }}
        run: chmod +x gradlew

      # 변경된 모듈 Build & Test
      - name: Run Build
        if: ${{ steps.analyze.outputs.TASKS != 'none' }}
        run: |
          ./gradlew ${{ steps.analyze.outputs.TASKS }} \
            --parallel \
            --build-cache

      # Gradle build task에는 테스트 실행도 포함되므로
      # 별도의 test task를 추가하지 않아도 됨

      # 빌드/테스트 실패 시 결과 업로드
      - name: Upload Test Report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          # 각 모듈의 테스트 결과를 모두 수집
          path: |
            **/build/reports/tests/**
            **/build/test-results/**

          # GitHub Actions에서 확인할 수 있는 Artifact 이름
          name: test-report